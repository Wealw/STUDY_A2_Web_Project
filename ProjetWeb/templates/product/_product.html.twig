{% if product.isOrderable == true %}
    <article class="product">
        <h2 class="product-name">
            {{ product.productName }}
        </h2>
        <h3 class="product-type">
            ({{ product.productType.productTypeName }})
        </h3>

        <img src="{{ product.productImagePath }}">

        <p class="product-price">
            {{ product.productPrice }} €
        </p>
        <br>
        <a href="{{ path('merch.show', {id: product.Id}) }}" class="link">Check This</a>
    </article>
{% endif %}


